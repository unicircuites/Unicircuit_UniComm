const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgres://postgres:postgres@localhost:5432/unicomm_db' }); async function wipe() { await pool.query('DELETE FROM wa_messages'); await pool.query('DELETE FROM wa_chats'); await pool.query('DELETE FROM wa_contacts'); console.log('Wiped all WhatsApp data (messages, chats, contacts)!'); pool.end(); } wipe().catch(console.error);
