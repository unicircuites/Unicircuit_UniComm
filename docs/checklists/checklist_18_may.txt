Checklist — 18 May 2026

## Tower Server — Network Drive Connection

### SMB / Network Share Connectivity
- Verified SMB connectivity between local system and Tower server
- Checked SMB server configuration using PowerShell
- Verified authentication issue during network share access
- Confirmed UCS recording storage path access from remote system
- Successfully connected network drive to Tower server recordings location
- Verified shared folder accessibility from application server

### Recording Access / Media Loading
- Fixed WhatsApp media loading issue
- Verified audio recording streaming via:
  - /api/calls/recordings/*
- Confirmed recordings playable in browser audio player
- Verified URL encoding for filenames containing +

---

# PBX Call Recordings — Duplicate Fix

## Problem Identified

### Symptoms
- Local system showing:
  - ~766 pages
  - ~15320 recordings

- Tower server showing:
  - ~383 pages
  - ~7666 recordings

### Root Cause

PBX_RECORDINGS_DIR pointed to:

PBX_RECORDINGS_DIR=C:\MatrixVMS\Voicemail_Backup

Matrix SARVAM UCS automatically creates timestamp-based backup folders:

16May_2026_175604
16May_2026_183002

Application recursively scanned ALL folders.

Same .wav recordings existed in multiple backup snapshots.

This caused duplicate recordings in UI.

---

# Investigation Steps Performed

## Verified Backend Route

Checked:

router.get('/recordings')

## Verified Recursive Scanner

Checked function:

getAllRecordingFiles(dir)

Confirmed:
- recursive directory traversal
- scans all subfolders
- pushes all matching audio files

## Verified Environment Variable

Command used:

type backend\.env | findstr PBX_RECORDINGS_DIR

Result:

PBX_RECORDINGS_DIR=C:\MatrixVMS\Voicemail_Backup

## Verified Folder Structure

Command used:

Get-ChildItem "C:\MatrixVMS\Voicemail_Backup" -Directory

Confirmed multiple timestamp backup folders existed.

---

# Permanent Deduplication Fix Applied

## Previous Logic

Old code directly used:

const files = getAllRecordingFiles(REC_DIR)

This loaded duplicate recordings from multiple backup folders.

---

## New Logic Implemented

### Added Deduplication by Filename

Implemented:

const allFiles = getAllRecordingFiles(REC_DIR);

const uniqueMap = new Map();

for (const fullPath of allFiles) {
  const filename = path.basename(fullPath);
  const stat = fs.statSync(fullPath);

  if (
    !uniqueMap.has(filename) ||
    stat.mtime > uniqueMap.get(filename).mtime
  ) {
    uniqueMap.set(filename, {
      fullPath,
      mtime: stat.mtime
    });
  }
}

### Result
- scans all UCS backup folders
- keeps only unique filenames
- preserves latest modified copy
- prevents duplicate UI records
- future-safe for unlimited backup folders

---

# Final Result

## Before Fix
- ~766 pages
- ~15320 recordings
- duplicate recordings visible

## After Fix
- ~371 pages
- unique recordings only
- no duplicate UI entries
- recordings preserved correctly

---

# Commands Used During Debugging

findstr /S /I /N "recording_file" *.js

findstr /S /I /N "total_pages" backend\*.js backend\routes\*.js backend\services\*.js

Get-ChildItem "C:\MatrixVMS\Voicemail_Backup" -Directory

CREATE UNIQUE INDEX IF NOT EXISTS idx_call_logs_recording_file_unique
ON call_logs(recording_file)
WHERE recording_file IS NOT NULL;

---

# Current Status

✅ Network drive connected successfully

✅ Recording playback working

✅ Duplicate recordings issue fixed

✅ Future backup folders handled automatically

✅ PBX recordings UI stable and production-ready


---

# Additional SMB Network Drive Setup Contributions

## Initial Problem Investigation
- Matrix SARVAM UCS network drive test was failing
- Error shown:
  - Connection fail, check network drive parameters
  - Invalid user or authentication fail
- Browser/UNC path testing initially failed:
  - \\DESKTOP-6J37NPS\MatrixVMS
  - \\192.168.0.205\MatrixVMS

These tests confirmed:
- SMB path existed
- authentication layer was failing
- issue was not folder creation related

---

## SMB User Creation

Created dedicated SMB authentication user:

```cmd
net user matrix 12345 /add
```

Purpose:
- dedicated SMB login for Matrix UCS
- avoided dependency on Microsoft/Outlook login

---

## Share Permission Verification

Verified:
- Everyone group added
- Full Control enabled

Verified Security permissions:
- matrix user added with Full Control
- NTFS inheritance working correctly

---

## NTFS Permission Validation

Verified using:

```cmd
icacls C:\MatrixVMS
```

Confirmed:
- matrix:(OI)(CI)(F)

---

## Firewall + Network Discovery Configuration

Commands executed:

```cmd
netsh advfirewall firewall set rule group="Network Discovery" new enable=Yes
```

```cmd
netsh advfirewall firewall set rule group="File and Printer Sharing" new enable=Yes
```

---

## Network Profile Configuration

Changed network profile to Private:

```cmd
powershell -Command "Set-NetConnectionProfile -NetworkCategory Private"
```

Purpose:
- allow SMB sharing/discovery properly over LAN

---

## SMB Local Authentication Compatibility

Registry fix applied:

```cmd
reg add "HKLM\SYSTEM\CurrentControlSet\Services\LanmanServer\Parameters" /v LocalAccountTokenFilterPolicy /t REG_DWORD /d 1 /f
```

Restarted SMB service:

```cmd
net stop server /y
net start server
```

Purpose:
- allowed remote SMB authentication using local Windows account

---

## SMB Connectivity Testing

### Port 445 Validation

Verified using:

```cmd
powershell "Test-NetConnection 192.168.0.205 -Port 445"
```

Result:
- TcpTestSucceeded : True

---

## SMB Remote Login Test

Test command:

```cmd
net use Z: \\192.168.0.205\MatrixVMS /user:192.168.0.205\matrix 12345 /persistent:no
```

Result:
- authentication successful
- drive mapped successfully

Verified using:

```cmd
dir Z:\
```

Result:
- remote SMB directory accessible successfully

---

## VMware Networking Investigation

Investigated whether VMware/virtual networking caused issue.

Findings:
- VMware networking was NOT root cause
- Windows-to-Windows SMB worked properly
- issue isolated specifically to Matrix UCS SMB authentication compatibility

---

## UCS Authentication Investigation

Tested multiple username formats:

matrix

and:

192.168.0.205\matrix

Observed:
- UCS was reaching SMB server successfully
- failures changed depending on auth format

This isolated issue specifically to:
- SMB auth negotiation
- legacy NTLM compatibility

---

## Final Working UCS Configuration

IP Address:
192.168.0.205

User Name:
matrix

Password:
12345

Shared Folder Name:
MatrixVMS

---

## Final Validation

Confirmed:
- SMB share accessible locally
- SMB share accessible remotely
- network drive mounted successfully
- Matrix UCS authenticated successfully
- recording/network storage functioning correctly

# Outlook Contacts Backup / Restore System — Full Work Log (18 May 2026)

## 1. Outlook Contacts Toolbar Integration

Added Outlook tools section inside Contacts toolbar:

* Refresh Outlook contacts
* Import Outlook contacts to CRM
* Sync CRM ↔ Outlook
* Mail shortcut button
* Backup button for Outlook contacts

Added toolbar block:

* `contacts-tools-outlook`

Integrated buttons with:

* `loadOutlookContactsDirectory()`
* `importOutlookContactsToCRM()`
* `syncOutlookContactsWithCRM()`

---

# 2. Outlook Backup Popup Modal UI

Created professional popup modal system instead of inline panel.

Added:

* dark blurred modal background
* centered modal card
* responsive layout
* professional backup cards
* scrollable backup list
* action buttons

Popup contains:

## Create Backup Section

* Create Backup button
* backup status text
* automatic refresh after creation

## Restore / Old Backups Section

* Refresh button
* backup listing
* restore button
* download button
* delete button
* preview/read button

---

# 3. Frontend JavaScript Functions Added

Implemented:

## Modal Controls

* `openOutlookBackupModal()`
* `closeOutlookBackupModal()`

## Backup Functions

* `createOutlookBackup()`
* `loadOutlookBackupList()`
* `downloadOutlookBackup()`
* `restoreOutlookBackup()`
* `deleteOutlookBackup()`
* `previewOutlookBackup()`

---

# 4. Professional UI Improvements

Added:

* translucent dark cards
* hover effects
* spacing fixes
* button alignment fixes
* modal overlay
* border radius consistency
* responsive layout
* professional button grouping

---

# 5. Backup Preview System

Added preview modal:

* tabular data preview
* contact inspection before restore
* scrollable table
* readable formatting

Preview shows:

* Name
* Email
* Phone
* Company
* Job Title

---

# 6. Warning / Confirmation Popups

Added confirmations before:

## Restore

Warning before overwriting contacts.

## Delete

Confirmation before permanent deletion.

Used popup confirmations for safer operation.

---

# 7. Backend API Development

Created:

* `backend/routes/outlookBackups.js`

Implemented APIs:

## GET

`/api/outlook-backups`

List all backups.

## POST

`/api/outlook-backups/create`

Create JSON backup.

## POST

`/api/outlook-backups/restore`

Restore contacts from selected backup.

## DELETE

`/api/outlook-backups/:file`

Delete backup file.

## GET

`/api/outlook-backups/download/:file`

Download backup.

## GET

`/api/outlook-backups/preview/:file`

Preview backup content.

---

# 8. Backup Storage System

Created folders:

## Main backup folder

* `backend/outlook_backups`

## Additional backup structure

* `backend/backups/outlook_contacts`

Backup naming pattern:

* `outlook_backup_YYYY-MM-DDTHH-MM-SS.json`

---

# 9. Restore Logic Improvements

Fixed restore issues:

* previously creating 0 KB backups
* restore not applying changes
* contacts array validation issue

Corrected:

```js
const contacts = req.body.contacts || [];
```

instead of unsafe direct assignment.

Added:

* overwrite handling
* refresh after restore
* automatic reload of Outlook directory

---

# 10. Dashboard Integration

Connected frontend with backend APIs using:

* fetch()
* async/await
* JSON handling

Integrated backup popup into dashboard workflow.

---

# 11. Refresh Backup List Fix

Fixed:

* backup list not updating after create
* refresh button not functioning

Implemented:

* automatic reload after create/delete/restore

---

# 12. Git Deployment Work

Tracked modified files:

* dashboard.html
* backend/server.js
* backend/routes/calls.js
* backend/routes/outlookBackups.js
* backend/services/matrixSmdr.js

Added backup folders to git tracking.

Performed:

* git add
* git status
* git commit
* git push

---

# 13. Tower Server Deployment

Pulled latest code on tower server.

Used:

* `git fetch`
* `git reset --hard origin/main`

Restarted PM2 service:

* `pm2 restart unicomm`

---

# 14. SSL / HTTPS Recovery Work

Problem:

* server crashing due to missing:

  * `server.crt`

Error:

```txt
ENOENT: no such file or directory
```

---

# 15. Certificate Recovery

Generated self-signed certificate system.

Created:

* `server.pfx`

Using PowerShell certificate commands.

---

# 16. HTTPS Server Migration

Changed HTTPS initialization from:

```js
key + cert
```

to:

```js
pfx + passphrase
```

Updated:

```js
https.createServer({
  pfx: fs.readFileSync(
    path.join(__dirname, 'certs', 'server.pfx')
  ),
  passphrase: 'StrongPass123!'
}, app);
```

---

# 17. PM2 Recovery

Recovered PM2 service from repeated crash loop.

Final status:

* HTTPS working
* PM2 online
* WhatsApp connected
* PBX ready
* API healthy

---

# 18. Final Stable State

System now supports:

✅ Outlook contact backups
✅ Restore backups
✅ Backup preview
✅ Download backups
✅ Delete backups
✅ Professional popup modal
✅ Restore confirmations
✅ Live refresh
✅ HTTPS working
✅ PM2 stable
✅ Tower deployment stable

Backend Fixes
Fixed SMDR parser syntax issues in matrixSmdr.js
Resolved missing } / broken record = {} block issue
Added recording_file support in parsed records
Fixed audio loading / media playback issue
Verified backend heartbeat + PBX listener running on port 5001
Recording System Improvements
Recursive Recording Scan

Updated backend so recordings are now scanned recursively from:

C:\MatrixVMS\Voicemail_Backup

instead of only one folder level.

Using:

function getAllRecordingFiles(dir)

inside:

backend/routes/calls.js
Folder Structure Support

You wanted recordings stored like:

16May_2026_175604
 ├── 21(SALES_TEAM)
 ├── 22(TECH_TEAM)

16May_2026_183002
 ├── 21(SALES_TEAM)
 ├── 22(TECH_TEAM)

instead of flat files.

We confirmed backend is now correctly reading nested folders like:

C:\MatrixVMS\Voicemail_Backup\16May_2026_175604\222(PAWAN_B)\file.wav

and serving them properly.

Verified from logs:

[RECORDING REQUEST]
C:\MatrixVMS\Voicemail_Backup\16May_2026_175604\222(PAWAN_B)\03042025_105702_CT_+917746943480_21.wav
API Changes

Backend /recordings API now returns:

relative_path
full_path
recursive folder file support

instead of filename-only logic.

Frontend Recording UI
Added:
Folder path display on each recording card
Relative path rendering from backend

Example shown:

16May_2026_183002\222(PAWAN_B)\05042025_132014_CT...
Pagination

Existing pagination preserved:

Prev / Next buttons
20 recordings per page
File Search / Debugging Work


